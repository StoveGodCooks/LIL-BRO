"""LIL BRO LOCAL roadmap engine.

Living roadmap data model, icebox capture, and brainstorm / plan /
execute prompt helpers that turn a user goal into a queued sequence
of tasks with explicit user approval gates.

All state is persisted as plain JSON under ``~/.lilbro-local/``.
"""
