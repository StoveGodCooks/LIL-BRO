"""LIL BRO LOCAL memory subsystem.

Optional ChromaDB-backed vector store for session memories,
project registry, context injection, and session summarization.

All components degrade gracefully when chromadb is not installed.
"""
